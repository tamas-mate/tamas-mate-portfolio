const LoadingSpinner = () => {
	return (
		<div className="flex-center h-screen">
			<div className="border-accent h-20 w-20 animate-spin rounded-full border-t-4 border-b-4"></div>
		</div>
	);
};

export default LoadingSpinner;
